version: '3.8'

services:
  mcp-tools:
    build: .
    container_name: mcp-tools
    ports:
      - "9888:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped